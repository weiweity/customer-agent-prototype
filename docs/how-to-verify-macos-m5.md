# macOS M5 人工核验清单

本页是 **macOS 合成查询体验的人工门禁**。由人在本机 Mac 上看窗口、点控件、读颜色与文案、查系统剪贴板。勾选只表示该项被真人观察并成立。

> **本页不是 M5 完成声明。** 写完、提交或合并本清单，都不等于 M5 已通过。未观察项保持空白。不得把 unit、CI、Playwright 或 adapter 测试的绿写成「Mac 人工已接受」。

相关文档：[如何验证桌面 Demo](how-to-verify-desktop.md) · [第一次运行](tutorial-first-run.md) · [执行清单](plans/2026-09-06-execution-goal.md)

---

## 0. 本页证明什么，不证明什么

| 本页是 | 本页不是 |
| --- | --- |
| 人眼 / 人手在真实 macOS 窗口上的观察清单 | 自动化回归、CI 门禁、Playwright 证据 |
| M5 待人工勾选的闸门 | M5 已完成、可外发、可试装客户 |
| 合成数据、本机 loopback、复制只进剪贴板 | 真实飞书、真实客户数据、代发、签名公证 |

下列命令 **可以** 用来启动本机合成栈或开发态客户端，但它们的退出码 **不是** 本页任何一项的通过证据：

- `pnpm test` / `pnpm lint` / `pnpm typecheck` / `pnpm check`
- `pnpm test:e2e` / `pnpm --filter @customer-agent/desktop exec playwright test …`
- `CUSTOMER_AGENT_API_PG15_INTEGRATION=1 pnpm --filter @customer-agent/api test:e2e:backend`
- `pnpm --filter @customer-agent/desktop exec vitest run …`

CI 绿、unit 绿、Playwright 绿，都 **不得** 勾选本页。本页也 **不得** 用来关闭 BACKEND-CI-503、治理 PR、Windows 实机或正式发包。

基线：产品 `main@6385c67`（含 #83 query-route / 非激活 palette；#85 为文档收口）。查询框不再选手动平台 / 品类 / SKU。M3 业务异常自动化（#71）已合入，仍 **不是** 本页 STALE 的人工证据。本页不打包、不签名、不公证、不合并、不部署、不自动外发，也不执行 `stack start`。

---

## 1. 开始前（仍不是本门禁）

本机环境：

```bash
export PATH="$HOME/homebrew/opt/node@24/bin:$HOME/homebrew/bin:$PATH"
hash -r
node -v    # 需要 v24.x
pnpm -v    # 项目锁定 11.19.0
```

合成栈必须已经在跑（隔离 PG15 + 合成身份 + API + worker + 已发布的合成话术）。启动与停止见 [如何验证 · 本机合成栈（M1）](how-to-verify-desktop.md#本机合成栈m1)。**当前若 `content_current` 已是 `rel_6` MENOKIN，不要 `stack.ts start`**，它会再种合成种子并把发布顶掉；只 `status`，API 必须是 `apps/api/dist/main.js`。检索开发浮窗见 [How to 启动检索浮窗](how-to-run-macos-semantic-query.md)。

```bash
node scripts/synthetic-stack/stack.ts status
```

客户端二选一，并在记录里写明走的是哪条：

| 路径 | 配置从哪来 | 何时用 |
| --- | --- | --- |
| 开发态（`启动客服Agent.command` 或 `stack.ts desktop` 打出的 `pnpm dev`） | 环境变量 `CUSTOMER_AGENT_DESKTOP_API_ORIGIN` / `CUSTOMER_AGENT_DESKTOP_IDENTITY_ORIGIN` | 日常人工观察 |
| M4 本机未签名包 | 应用 userData 下的 `synthetic-stack.json`；**打包态忽略上述环境变量** | 仅当本次真的打开了 M4 未签名 `.app` |

禁止：真实飞书 URL / 凭据、真实客户数据、把未签名包发给别人、购买或使用签名公证、Windows 包。

每项只许填：**通过** / **未通过** / **未观察**。未触发的失败路径填 **未观察**，不要填通过。

---

## 2. 登录：成功不是红色校验条

观察对象是 Query 胶囊当前 UI，不是 Playwright `data-testid` 断言。v0.3.1 起胶囊按钮是「登录」，未登录靠输入框占位，不再画单独的 unsigned / success 提示条。第 7 节 2026-09-16 观察仍按当时文案记录，不改历史勾选。

当前人眼合同：

| 状态 | 现在应看到 | 不得与之混淆 |
| --- | --- | --- |
| 未登录 | 按钮「登录」；占位「登录后查询话术」。点查询**不**再出「请先合成登录」横幅 | 输入校验红字、失效/失败条 |
| 已登录 | `{角色} · 退出`；占位「输入或粘贴客户问题，回车查询」。**没有**「合成登录成功…」成功条 | `.validation-error` 红字 |
| 失效 `expired` | `登录已失效，请重新登录`（危险色） | 未登录占位、登录失败 |
| 失败 `failed` | 例如关掉登录窗 → `操作已取消`；超时/身份方不可达 → `服务暂不可用，请重试`（危险色） | 未登录、失效 |

人工步骤：

1. 打开 Query。未登录时占位是 `登录后查询话术`。点查询不得出现输入校验红横幅，也不得再出现 `请先合成登录` 提示条。
2. 点「登录」，走独立窗口「登录」（loopback 身份，禁止真实飞书）。成功后：
   - 按钮变成 `{角色} · 退出`（合成坐席一般为 `agent`）；
   - 悬停可见到期时间；
   - **不得** 占用输入框下的红色 `invalidMessage`，也**不得**再画成功色 notice。
3. 关掉登录窗或让登录失败：提示为失败 `kind`，文案与未登录占位、失效都不同。
4. 已登录会话恢复（例如收起再打开 Query，或进程仍持有加密会话）：不得残留红字成功条，也不得把恢复误报成一次新的失败。
5. 点退出：立即回到未登录，按钮回到「登录」，占位回到 `登录后查询话术`。这不是 `expired`。
6. 若能等到到期，或会话已被撤销：提示必须是 `登录已失效，请重新登录`，不得仍显示成功条，也不得写成从未登录过的占位口吻。人工已见该失效文案；Query 保持打开时，约一轮状态轮询后会回到未登录占位。后出现的未登录口吻记备注，**不**否定失效曾经出现，也**不**把四种提示整项勾成通过（关窗失败仍未观察）。

勾选：

- [x] 登录成功是非红色成功提示，不是红色无效横幅
- [ ] 未登录 / 成功 / 失效 / 失败 四种提示可被人眼区分（**部分观察**：未登录、成功、失效已见；关窗取消失败未触发。合成授权会立刻成功）
- [x] 恢复已登录会话没有残留红字
- [x] 未把 unit / Playwright 登录用例当作本项通过

---

## 3. 查询范围：问句路由，禁止手选平台/品类/SKU

查询框不要平台、品类、具体款下拉。范围由问句 intent 决定：发货/地址/售后走全店；活动走活动类；问句点到合成款名才绑 SKU。界面不得再选手动范围，也不得出现「合成商品标识」手填框。

人工步骤：

1. 登录后查询「什么时候发货」。应出全店发货话术，**不要**先选手动平台或商品。
2. 查询「澄芽氨基酸洁面乳怎么用」。若有对应 SKU 话术可命中；对不上也不得弹出商品下拉让人补选。
3. 界面确认：没有「查询平台」「商品范围」「查询品类」「查询具体款」。

勾选：

- [x] 查询框没有平台 / 品类 / 具体款下拉
- [x] 界面没有商品标识手填框
- [x] 登录后直接查「什么时候发货」能出全店话术
- [x] 未把 Playwright 目录用例当作本项通过

---

## 4. 复制只进剪贴板；无命中辅导；合成标注可见

复制成功的按钮/toast 文案是 `已复制`。合同写明：复制成功只表示写入系统剪贴板，不表示发送、采纳、正确或问题已解决。界面不得出现「已发送」。

人工步骤：

1. 命中一条候选，点「复制话术」（或数字键 1/2/3）。按钮变为「已复制」。
2. 在「文本编辑」或其它应用里粘贴：必须是刚才那条话术原文。
3. 确认没有把内容发到飞书、聊天工具或任何远端。本 Demo 无代发。
4. 话术卡不再贴 `DEMO · 合成数据`。登录入口是「登录」。胶囊**不再**贴 `DEMO` / `MOCK AUTH` 徽标。有占位符时输入框标签是「订单号」或「日期」，填的仍是合成值。
5. **无命中辅导**：登录后查询一条合成栈不会命中的问题（不要用真实客户原话）。应看到「没找到可用话术」，以及请客户稍等、联系话术师/运营的辅导。若出现「复制合成联系方式」「打开合成入口」：
   - 复制联系方式只把合成联系卡写入剪贴板；
   - 打开入口只表示入口窗口已打开；
   - 状态停留在「待核实」/「已打开入口」/「已复制联系方式」；
   - 明确 **不是** 转交回执，不得理解成已经转交成功。

勾选：

- [x] 复制后系统剪贴板确有话术原文，且界面只说「已复制」
- [x] 全程没有发送、没有「已发送」
- [x] 合成标注在登录、查询范围、候选卡上均可见（合成登录 / 合成联系卡已见）
- [x] 无命中辅导可见；打开入口 / 复制联系方式不被当成转交成功
- [x] 未把复制相关 unit / E2E 当作本项通过

---

## 5. 回退后的 STALE：不能因为还能搜到发货话术就勾

历史误判：有人回退后再次查询「什么时候发货」仍能命中，就把 STALE 写成已人工验证。那只证明 **回退后查询还可用**。回退到仍含该话术的版本时，重新搜索命中是预期现象， **不能** 证明旧候选已失效。

STALE 要看的是：**已经拿到的那条旧候选** 在回退后不得再被当成有效稿复制。产品文案是 `内容已变化，请重新查询`（错误码 `STALE`）。发布被替换时，Query 也可能先出现 `当前版本已失效，请重新核验`。

人工步骤：

1. 登录后直接查询「什么时候发货」（不要选手动平台或商品），复制一条候选（只进剪贴板）。 **不要关掉这条结果。**
2. 用合成 Owner 在 loopback 上对当前发布做一次回退（`POST /v1/content/rollback`）。本清单不实现回退。没有**已批准**的安全合成 Owner 回退入口时，本项记 **未观察**。禁止 `stack start` 顶掉 `rel_6` 来制造 STALE。
3. 回到 **同一条旧结果**：再点复制。必须失败，提示内容已变化 / 当前版本已失效，而不是再次「已复制」。
4. 允许重新查询。重新搜「什么时候发货」 **仍可能命中**——把这一步单独记「回退后仍可查询」， **禁止** 用来勾选 STALE。
5. 禁止用 `backend-runtime.e2e.test.ts`、Playwright 或任何 adapter 复制 STALE 断言代替本项。

勾选：

- [x] 回退后，旧候选再复制被拒绝（STALE / 版本已失效）
- [x] 没有仅仅因为同一句发货话术还能搜到就勾选 STALE
- [x] 未把 D5 / M3 自动化当成 Mac 人工 STALE 通过

---

## 6. 若本次打开的是 M4 未签名包

仅当真人双击/打开了 M4 产出的本机未签名 macOS 应用时填写。本页 **不授权** 执行 `pnpm package:mac:local` 或 `pnpm package:mac`，不签名、不公证、不外发。未打开该包则整节记 **未观察**。

期望（观察，不是再打一次包）：

| 项 | 通过标准 | 不能当成 |
| --- | --- | --- |
| 产物身份 | 文件名含 `UNSIGNED`；来自 `release/local-unsigned/` 一类本机证明目录 | 正式外发包、已签名、已公证 |
| 配置 | `~/Library/Application Support/客服话术浮窗 Demo/synthetic-stack.json` 存在，且由合成栈写入 | 打包态读环境变量 |
| 文件内容 | `mode` 为 `synthetic-local`；`apiOrigin` / `identityOrigin` 都是精确 `http://127.0.0.1:<端口>` | 飞书、非 loopback、带路径/账号的 URL |
| 启动 | 栈已 `start` 的前提下，未签名 `.app` 能走第 2～4 节主链 | Gatekeeper 对外部下载放行、可发给别人 |
| 环境变量 | 打包态即使误设 `CUSTOMER_AGENT_DESKTOP_*` 也必须忽略 | 用开发态 env 冒充打包路径已验 |

`codesign --verify` 对本机 UNSIGNED 包预期失败，只能证明未签名，不能证明 M5 通过。

勾选：

- [x] 本次确实打开了 M4 未签名 `.app`
- [x] userData 中的 `synthetic-stack.json` 被该包使用，而不是环境变量
- [x] 未把未签名包写成已签名或可外发
- [x] 未跑正式 `package:mac`、未签名、未公证、未外发（本机 `package:mac:local` 只为打开 UNSIGNED `.app`）

---

## 7. 记录（收口时仍不得把 M5 标完成）

| 项 | 通过 / 未通过 / 未观察 | 观察人 | 日期 | 走的客户端（dev / M4 UNSIGNED） | 备注 |
| --- | --- | --- | --- | --- | --- |
| 登录成功非红；四种会话提示可区分 | 部分通过：失效 **通过**；关窗失败 **未观察** | hutou | 2026-09-15 / 2026-09-16 | dev | 成功非红；恢复会话无红字；退出为未登录。2026-09-16 Query 保持打开，已批准 `stack.ts anomaly session-revoke` 后出现「登录已失效，请重新登录」（危险色）；约一轮状态轮询后变成「请先合成登录」。后变未登录记备注，不否定失效观察。同日再试关窗：登录窗未出现；Command+W 只把 Query 收起，回来后是「合成登录成功」，未见「操作已取消」 |
| 问句路由；无手选平台/品类/SKU；无手填商品 ref | 通过 | hutou | 2026-09-15 | dev | #83 后查询框无范围下拉。全店「什么时候发货」命中 |
| 目录缺失 fail-closed；全店仍可用 | **未观察** | hutou | 2026-09-15 | dev | 产品已改为不问坐席选手动品类；本项不再适用点选 fail-closed |
| 复制=剪贴板；无命中辅导；合成标注 | 通过 | hutou | 2026-09-15 | dev | 复制只进剪贴板。无命中「虚构星球食堂…」。联系卡与求助入口已见，不是转交回执 |
| 回退 STALE（不是「还能搜到发货」） | **通过** | hutou | 2026-09-16 | M4 UNSIGNED | Query 保持打开、旧发货候选仍在时，已批准 `POST /v1/content/rollback` 目标 `rel_5`（新 current `rel_16`，11 条种子）。旧候选点「复制话术」出现「内容已变化，请重新查询」。不是重新搜索后的无命中。未 `stack start`。随后回退 `rel_6` 接回 MENOKIN（`rel_17`，400 条） |
| M4 未签名包 + `synthetic-stack.json` | **通过** | hutou | 2026-09-16 | M4 UNSIGNED | 打开 `release/local-unsigned/mac-universal/客服话术浮窗 Demo.app`（文件名含 UNSIGNED；`codesign --verify` 失败）。首包无仓外索引环境变量，leftover `/v1/search` 对「什么时候发货」为 no_hit。补打包态默认 hydrate/index 后重打，合成登录后命中全店发货话术（青岛/郑州保税仓、72 小时）且能复制。origins 来自 userData `synthetic-stack.json`。禁止外发。不是签名/公证，不是 M5 整页通过 |

收口规则：

- 任一项未观察或未通过 → **M5 仍为人工待验**。
- 即使上表全填通过，也须由负责人在仓外验收记录里签字；本文件勾选不是完成戳。
- 不得在执行清单、CHANGELOG、PR 标题或提交说明里把 M5 写成 done / 已接受。
- 本页存在于仓库里，只表示门禁标准已写明，不表示门禁已通过。

---

## 8. 明确不在本门禁

- Windows 实现、Windows 包、Windows 实机
- 治理 PR #80（保持 OPEN）、GitHub Pages
- 冻结合同、旧 migration、真实飞书 / 客户数据
- 付费签名、公证、部署、自动外发、代发
- Dock / Cmd+Tab / 台前调度 / 真实全局快捷键（仍是设备门，但不是本页 M5 五项）
- 把本清单合并进 `main` 也不等于 M5 完成

---

## 9. 剩余人工步骤（仍待本机操作；本页不代勾）

下列只列出 **尚未观察** 且必须由人在真实 macOS 上做的步骤。已勾选项不要重做来冒充新的通过。不要启动新的 synthetic stack。不要把 unit / CI / Playwright 写成通过。

开始前只做：

```bash
export PATH="$HOME/homebrew/opt/node@24/bin:$HOME/homebrew/bin:$PATH"
hash -r
node -v    # v24.x
pnpm -v    # 11.19.0
node scripts/synthetic-stack/stack.ts status
```

`status` 必须显示隔离 PG15 + 合成身份 + API（`apps/api/dist/main.js`）在跑。当前发布是 MENOKIN 快照的后续 seq（`rel_17` 复制 `rel_6`，400 条），**不要** `stack.ts start`。开发态启动见 [How to 启动检索浮窗](how-to-run-macos-semantic-query.md)。

| 你必须在本机做的事 | 通过标准 | 现在状态 | 禁止 |
| --- | --- | --- | --- |
| 打开 Query，点「合成登录」，在登录窗出现后关掉它 | 失败提示是 `操作已取消`（失败 `kind`），不是未登录红横幅，也不是成功色 | **未观察**（2026-09-16：登录窗未出现；Command+W 只收起 Query，回来是「合成登录成功」） | 用自动化登录用例勾选 |

做完后只改第 7 节对应行的「通过 / 未通过 / 未观察」、观察人、日期和客户端路径。任一项仍未观察或未通过 → **M5 仍为人工待验**。本文件更新不是完成戳。
