# Linux 打包态远端主链 how-to（未观察）

> **状态：** 实现中（`feat/linux-packaged-remote-howto`）。  
> **不包含：** 新增 `package:linux`、Linux 实机勾选、把本页当成已交付。

## 拍板

与 macOS / Windows 远端页对称，路径用 `~/.config`。写明当前**没有** Linux UNSIGNED 打包脚本，所以本页不能当产物证据。
