# P9：新 profile 下 M5 主链须重验

> **状态：** 口径已写入 `docs/how-to-verify-macos-m5.md` §7.1。  
> **不包含：** 把 M5 标完成、再打 `package:mac:local`、用 `synthetic-offline` 勾登录/查询/复制、Windows 实机。

## 拍板

1. 第 7 节历史勾选只对当时的 `synthetic-local`（dev 与 M4 UNSIGNED）有效。
2. `synthetic-offline` **不适用** M5 主链。
3. `product-remote` 适用，但本页全部 **未观察**：远端登录/关窗/断网、查询复制、STALE、干净 userData 打包态。
4. 网络错误不得记成「登录已失效」。
5. 本刀不授权打包、不授权飞书实机勾选、不把 CI 绿写成通过。

## 明确不做

- 不改第 7 节已有日期与勾选
- 不声称 P4 / P6 / P7 本地提交等于 M5 远端已验
