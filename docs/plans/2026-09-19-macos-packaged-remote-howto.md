# macOS 打包态远端主链 how-to（未观察）

> **状态：** 实现中（`feat/macos-packaged-remote-howto`）。  
> **不包含：** 代填结果、现在打 `package:mac:local`、把 M5 标完成。

## 拍板

与办公机 Windows 远端页对称。配置在 Application Support。全部 **未观察**。须合入 P4/P7 后再打新包。
