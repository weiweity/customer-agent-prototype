# B4 负责人承接版本化装包入口

> **状态：** `APPROVED · SYNTHETIC IMPLEMENTATION ONLY`
> **实施：** `LOCAL VALIDATED · PR HANDOFF PENDING`
> **依据：** 用户 2026-09-06 授权逐功能 PR 自主实施；基于 PR #30。真实资料读取、批准签发、T5/T6 与部署继续按各自阶段门管理。

比较仓外 Python 复制摘要算法与产品 test-support 提供确定性装包器两种边界。采用后者：内容身份模块继续拥有摘要，装包器拥有批准记录绑定、五文件生成、完整读取校验与失败清理。现有历史脚本和历史包不改写。

输入为已规范化内容、显式 manifest 元数据、cases/expectations 和独立批准记录。装包器不签发批准，不自动续期，不填补业务内容；只派生 question/content 摘要、审核字段、文件描述符和来源绑定摘要。外部锚点必须由调用方提供。输出使用仓外私有目录下的唯一新目录，通过现有闭合读取器后才返回成功；失败仅清理本次创建目录。

验证覆盖实际合成包往返、外部锚点、正文篡改、冲突、期限、非法元数据、目录权限与旧目录保护；追加 E0 PG15 全链、全仓检查与独立只读 review。所有证据保持 SYNTHETIC / NOT_EVALUATED。

## 实际验证（2026-09-06）

- 装包专项 18 项通过：同一规范化内容生成逐字节相同的五文件包；操作入口实际运行；外部锚点、正文/冲突/期限、派生字段、漏增、权限、别名与额外文件拒绝；旧包及失败前后父目录保持原样。
- E0 专项 69 项通过，包含 7 项 PG15 合成集成；无 DDL 或生产 API 变更，B1/B2 数据库与 API 专项不重复。
- 全仓测试 contracts 17、database 19、API 161、desktop 509、产物边界 8 通过。默认跳过 26 项显式入口/integration，其中 E0 和装包入口已另行运行。
- lint、typecheck、build、workspace:check 与 diff 检查通过。独立只读 review 无待修项。
- 未读取真实资料，未运行真实装包或 T5/T6；未运行桌面 E2E（桌面无变更），未合并或部署。
