# 检索说明：产品模式按 origin 分文件

> **状态：** 已实现（`feat/docs-retrieval-origin-explain`）。  
> **不包含：** 改检索实现、配隧道、push。

## 拍板

1. `explanation-desktop-retrieval` 写明产品模式 `retrieval-*.<origin>.json`。
2. 合入顺序文档标题改为「合入顺序记录」，不再写「本地未 push」。
