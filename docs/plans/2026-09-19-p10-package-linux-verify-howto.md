# P10：how-to-verify-desktop 写明 package:linux

> **状态：** 实现中（`feat/p10-package-linux-verify-howto`）。  
> **不包含：** 在 macOS 上打 Linux 包、Linux 实机勾选。

## 拍板

`how-to-verify-desktop` 增加 `package:linux` 行与 §3.4。写明必须在 Linux 上跑、本机不得把脚本存在当成产物已打出。W6 候选产物小节顺延为 §3.5。
