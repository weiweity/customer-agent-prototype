# 合入顺序：P7 / Linux 尖端刷新

> **状态：** 已实现（`feat/merge-order-p7-linux-tips`）。  
> **不包含：** 授权 push / 开 PR / 合 main。

## 拍板

1. P7 采用线尖端写成 `feat/p7-semantic-origin-path` `3878e4b`。
2. Linux 打包/CI/XDG 尖端仍是 `feat/p10-linux-xdg-userdata` `4f12bfc`。
3. `feat/p7-embeddings-delivery` 仍不要合。
