# 未明确问题的服务请求边界

> APPROVED · SYNTHETIC DEVELOPMENT / TEST ONLY。用户在剩余误返回待办后明确“可以，先处理”；沿用本阶段实现、验证及 commit/push/PR 授权。新 PR 合并、下一真实运行及业务/QA签收另行批准。
> 基线：PR #45 合并 `69c6044fd2ac097131fd325b1df94550c85b1064`。

## 问题与所有者

007 已单次完成：正例20/20、安全12/12、稳健性17/18，仍有1项应无结果却返回候选。仓外仅作结构核对：宽泛服务类别与通用处理请求没有确定问题；没有商品上下文。此结构提示现有信息不足门缺口，不构成业务/QA签收，也不将真实题面写入回归。

独立合成输入在现有 SearchBackend 中复现：存储问句可与宽泛请求完全匹配，使某条具体答复被展示。修改前6个文本边界用例和1个服务用例失败。

信息不足的所有者仍为 `search-text.ts:isEntityFreeGenericSearchText`，由 SearchBackend 转换为已有 `suppressMatches`；repository 继续先核验来源并保留 release 上下文。只增加整句结构判断，不在候选内容中推断“这个问题”指什么，不更改打分阈值或把新词全局剥离。包含商品、故障、退款等具体内容的请求必须继续进入既有判断。

## 验证

- 6类纯合成信息不足问法、6类具体商品/故障/操作对照。
- 服务回归包含会精确匹配的泛化存储问句；真实PG测试验证无候选、具体题目正常命中及来源撤销仍失败关闭。
- lint/typecheck/test/build，实验源码pin及原冻结验收，PG/HTTP集成，workspace/docs与实际候选包后验。
- 只刷新当前产品base/hash，原冻结题集及期望不改。

## 后续与限制

007的历史结果保持不变，修后合成结果不是008真实复验。下一真实运行需要新run_id、有效输入和明确单次授权；业务/QA仍需审阅剩余项及其处置，T6和下游动作评估未完成。此处采用有界整句语法，不声称覆盖全部自然语言或多轮上下文。
