# P10：how-to 补 Linux userData

> **状态：** 实现中（`feat/p10-linux-userdata-docs`）。  
> **不包含：** 包内后端、Linux 实机勾选。

## 拍板

Electron Linux userData 是 `~/.config/<appName>`。`stack start` 与 P4 打包态 how-to 必须写这一行，避免只提 macOS / Windows。
