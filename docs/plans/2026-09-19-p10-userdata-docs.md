# P10：how-to 写明 Windows userData 路径

> **状态：** 实现中（`feat/p10-userdata-docs`）。  
> **不包含：** 包内后端、改 M5 已有 macOS 勾选行。

## 拍板

`how-to-verify-desktop` 的 `stack start`、以及 P4 打包态 JSON，必须同时写 macOS Application Support 与 Windows `%APPDATA%`。历史决策文档加注，不删原文。
