# P10：M5 表注明 Windows/Linux userData 但不改勾选

> **状态：** 实现中（`feat/p10-m5-userdata-note`）。  
> **不包含：** 把 M5 标成 Windows/Linux 已通过、再打 `package:mac:local`。

## 拍板

第 7 节配置行仍是 macOS Application Support。补一句：Windows `%APPDATA%`、Linux `~/.config` 是打包客户端路径，**不是**本节通过证据。
