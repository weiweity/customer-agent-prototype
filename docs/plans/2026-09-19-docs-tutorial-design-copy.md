# 教程 / DESIGN：无后端口径与胶囊身份芯片

> **状态：** 已实现（`feat/docs-tutorial-design-copy`）。  
> **不包含：** 把打包态远端勾成已观察、改 beian、bump VERSION。过敏 SOP 按合成可选切片处理，不列入打包远端必勾。

## 拍板

1. `tutorial-first-run` 的「无后端」只描述默认合成离线路径和 Dashboard 演示环境。
2. DESIGN：查询胶囊会话芯片为「姓名 · 退出」，禁止 RBAC 角色；Dashboard「无后端 · 不保存」仍只约束工作台演示环境。
3. macOS 打包态 how-to 允许用合入后的 `package:mac:local` 新包，不再写「现在不准打」。
