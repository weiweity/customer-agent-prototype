# P7：有 API origin 时只写带后缀的检索文件

> **状态：** 实现中（`feat/p7-seed-keyed-catalog`）。  
> **不包含：** 删除本机未带后缀的旧文件、多公司 PG 分库。

## 拍板

1. 只读查找仍可回退未带后缀的文件（CLI / 本机旧目录）。
2. 产品模式一旦有 `apiOrigin`，env 永远指向带后缀的路径。
3. 带后缀文件还不存在、未带后缀文件存在时，**拷一次**再指向拷贝。之后登录 persist 只改带后缀文件。
4. 已有带后缀文件时不覆盖。
