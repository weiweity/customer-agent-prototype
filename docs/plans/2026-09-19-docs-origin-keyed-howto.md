# How-to：产品模式仓外文件带 origin 后缀

> **状态：** 已实现（`feat/docs-origin-keyed-howto`）。  
> **不包含：** 改检索实现、配隧道、把远端清单标成已观察。

## 拍板

1. `how-to-run-macos-semantic-query` 的默认文件表写带 origin 后缀的路径。
2. 未设 origin 时 leftover 未带后缀文件仍可用。
3. `how-to-verify-desktop` 的检索 CLI 行注明有 origin 时带后缀。
