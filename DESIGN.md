# 客服 Agent Demo 视觉基线

> 状态：首版 Demo 冻结骨架。品牌精修可迭代，但以下信息层级、状态语义与交互原则不得自行改写。

## 1. 产品印象

第一眼应该是：**最快找到正确话术并安全复制的坐席工作台**。

它不是聊天机器人、营销落地页或普通 IM 对话框。界面应专业、紧凑、安静，适合客服每天长时间使用；信息密度高，但不能拥挤。

## 2. 窗口与结构

- 默认窗口：`520 × 760`，最小 `480 × 640`，Windows 优先但可在 macOS 开发运行。
- 单窗口、两种只读视图：`话术浮窗`、`近期记录`。
- 首屏结构从上到下：
  1. 产品名、连接状态和环境徽标；
  2. 客户问题输入区与主搜索按钮；
  3. 状态提示或 Top 3 结果；
  4. 底部视图切换与键盘提示。
- 环境徽标必须持续可见：`DEMO`、`MOCK AUTH`、`SYNTHETIC DATA`。
- 在 520×760 首屏里，输入框和至少前两张结果卡的关键内容必须可见；不要用大面积 Hero 或装饰留白。

## 3. 视觉语言

### 颜色

```css
--canvas: #f3f5f4;
--surface: #ffffff;
--surface-subtle: #eaf0ee;
--ink: #17201d;
--muted: #5d6a65;
--border: #d6deda;
--accent: #0e766e;
--accent-hover: #095f59;
--focus: #1d63d5;
--success: #16794f;
--warning: #a66313;
--danger: #b93838;
```

- 主色只用于主操作、选中态和轻量强调。
- 风险、过期和无命中不能只靠颜色传达，必须有文字或图标标签。
- 禁止渐变、玻璃拟态、紫色主调、霓虹光晕和装饰性背景 blob。

### 字体

- 正文：`"Noto Sans SC", "Microsoft YaHei UI", sans-serif`。
- 英文与元数据：`"IBM Plex Sans", "Noto Sans SC", sans-serif`。
- ID、快捷键、短元数据可用 `"IBM Plex Mono", monospace`。
- 正文基准 14px；话术正文 14–15px / 1.55；辅助信息不小于 12px。

### 间距与形状

- 4px 网格；常用间距为 8、12、16、20、24。
- 容器圆角 10px，输入框与按钮 8px，徽标 4–6px；不要所有元素都做胶囊。
- 边框优先于阴影；阴影仅用于浮层反馈，且要克制。
- 动画 120–180ms，只服务状态变化；遵守 `prefers-reduced-motion`。

## 4. 关键组件

### 问题输入区

- 标签明确为“客户问题”，不能伪装成聊天输入框。
- 支持多行粘贴，提供清空按钮。
- 主按钮文字为“查找话术”；搜索中显示真实 loading 并禁止重复提交。
- 提示快捷键：`⌘/Ctrl + Enter` 搜索。

### Top 3 话术卡

每张卡必须显示：

- 排名（1、2、3）与匹配分数；
- 话术正文原文；
- 领域/平台/适用范围；
- 风险等级、有效期或活动状态；
- 明确的“复制话术”按钮与 `1/2/3` 快捷键。

正文是视觉主角，元数据紧凑排布，不能让标签盖过答案。高风险项需要清晰 warning，但不能制造误导性的“AI 正确率”。

### 状态与反馈

- 初始态：给出 2–3 个合成示例问题，点击可填入。
- 搜索中：显示局部 skeleton 或进度文案，不清空输入。
- 无命中：明确提示“未找到可用话术”，提供“转人工话术师/查看飞书源”的说明性下一步，但 Demo 中不要伪装成已接通真实系统。
- 复制成功：toast/aria-live 文案“已复制到剪贴板”，绝不能写“已发送”“已采纳”或“已解决”。
- 错误：保留输入，给出可重试动作。

### 近期记录

- 只展示本次进程内的脱敏问题摘要、时间、结果数、复制排名或无命中状态。
- 不持久化完整问题、答案或最终发送文本；重启后清空。
- 空状态要解释“仅保留本次 Demo 会话中的可证明事实”。

## 5. 可访问性与键盘

- Tab 顺序与视觉顺序一致；所有可操作元素都有可见 focus ring。
- `1/2/3` 仅在结果视图且焦点不在输入控件时触发复制。
- `Esc` 清除临时 toast，不得清空用户输入。
- 复制反馈使用 `aria-live="polite"`。
- 对比度至少满足 WCAG AA；交互目标最小 36px。

## 6. 明确禁止

- 不生成自由回答，不显示“AI 正在思考”。
- 不展示虚构的登录头像、真实公司成员或生产连接状态。
- 不使用真实产品/客户原文；fixture 必须显式为合成数据。
- 不把 copy 事件解释为发送、采纳、正确、解决或自动学习。
- 不在首版加入复杂 Dashboard、数据导入、内容发布、权限后台、训练配置或模型选型。
